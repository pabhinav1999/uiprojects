import { PlacholderDirective } from './placholder.directive';

describe('PlacholderDirective', () => {
  it('should create an instance', () => {
    const directive = new PlacholderDirective();
    expect(directive).toBeTruthy();
  });
});
