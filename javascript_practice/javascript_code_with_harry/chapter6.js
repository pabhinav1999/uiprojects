//write a program using prompt function to take input of age as a value from user and use alert to tell him if he can drive

let age = prompt("Enter your age:");
age = Number.parseInt(age);
if(age < 0)
console.error("Invalid age entered");
age > 18 ? alert('You can drive') : alert('You cannot drive')
let wantsToSeePrompt = confirm("Do you want to see prompt again?");

let num = prompt("Enter a number:");
num = Number.parseInt(num);
if( num > 4)
location.href="https://www.cricbuzz.com/";

let color= prompt("Enter a color");
document.body.style.backgroundColor = color;